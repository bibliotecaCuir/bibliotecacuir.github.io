name: Generar páginas de activaciones

on:
  push:
    branches: [main]
    paths:
      - "datos/activaciones.yaml"
      - "assets/portafolio/activaciones/**"
      - "scripts/generar-activaciones.js"
      - "scripts/lib/generar-portafolio.js"
      - "scripts/generar-drive-manifest.js"

permissions:
  contents: write

jobs:
  generar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generar portafolio/activaciones/*.html desde datos/activaciones.yaml
        run: node scripts/generar-activaciones.js

      - name: Generar assets/portafolio/drive-manifest.json
        run: node scripts/generar-drive-manifest.js

      - name: Commitear páginas generadas
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add portafolio/activaciones assets/portafolio/drive-manifest.json
          if git diff --cached --quiet; then
            echo "Sin cambios que commitear."
          else
            git commit -m "Generar páginas de activaciones automáticamente"
            git push
          fi
